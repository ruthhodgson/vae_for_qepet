# SummerProject2026
This repository takes the CNN model that it is forked from and changes this to a VAE to then compare how these two models can be used to train data from a geant simulation. The edited file at present is only that entitled '~ruth_vae_edits', all other files are as the original repo.
